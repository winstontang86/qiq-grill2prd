---
name: qiq-grill2prd
version: 1.0.0
description: 接收用户输入的产品需求描述、零散想法或原始 PRD 草稿，结合当前代码仓库的实际情况（领域术语 CONTEXT.md、架构决策 ADR、模块结构、已有功能），对其中的所有疑点、模糊点、矛盾点逐一进行严格的灵魂拷问，直到所有未确定的内容全部清晰化，最终在仓库根目录的 .qiqskills/grill2prd/ 目录下输出一份高质量的 final-prd.md 文档。Use when 用户提到"我想做一个新功能"、"帮我把这个想法整理成 PRD"、"输入了一份草稿/需求文档想完善"、"qiq-grill2prd"、"grill2prd"，或希望对一个产品想法进行系统化的需求澄清并产出落地文档时触发。
---

# qiq-grill2prd

把"模糊的产品想法 / 原始需求文档"通过结合代码仓库实情的严格盘问，转化为一份清晰、完整、可落地的 `final-prd.md`。

融合两种风格：`grill-with-docs` 的"逐节点灵魂拷问 + 与代码/文档现状交叉验证"的过程严谨性，加上 `to-prd` 的"标准 PRD 结构 + 模块/测试决策"的产出完备性。

## 核心理念（必须内化）

1. **不能放过任何一个模糊点**：用户输入几乎一定包含未明示的假设、术语歧义、未考虑的边界。本技能的职责就是把它们一个一个挖出来。
2. **每次只问一个问题**：等用户答完再问下一个。多个问题会让用户避重就轻。
3. **能查代码就查代码，绝不打扰用户**：可以通过读代码、`README`、`CONTEXT.md`、ADR 自行解决的疑问，**不要**去问用户。
4. **每个问题先给推荐答案**：先说"我倾向于 X，因为……"，再问"你怎么看？"。
5. **盘问完即产出**：所有疑点厘清后，立刻按模板生成 `final-prd.md`，不要再来回。

## 工作流程总览（4 个 Phase）

```
Phase 1  接收输入 + 扫描仓库 + 输出"仓库认知摘要"等用户确认
   ↓
Phase 2  按 10 个 branch 的问题树逐个拷问，遵守拷问硬性规则
   ↓
Phase 3  判定是否所有疑点已厘清；未达标则回 Phase 2 继续追问
   ↓
Phase 4  按模板写 .qiqskills/grill2prd/final-prd.md，告知用户路径
```

---

## Phase 1 — 接收输入并扫描仓库现状

1. **吃下用户输入**。可能是一两句话的想法、口语化描述、或一份粗糙的 PRD 草稿（文件路径或直接贴在对话里）。先完整读一遍，初步识别：要解决什么问题、目标用户是谁、核心动作是什么。

2. **自动扫描仓库**（**不要为这些事情打扰用户**）。详细清单见 `references/repo-scan-checklist.md`，关键点：
   - `README.md` / `CLAUDE.md` / `AGENTS.md` —— 项目大方向
   - `CONTEXT.md` / `CONTEXT-MAP.md` —— 领域术语字典（**最重要**，PRD 必须用这里的规范名词）
   - `docs/adr/` —— 已记录的架构决策
   - 依赖清单（`package.json` / `go.mod` / `pyproject.toml` 等）
   - 主目录结构 —— 已有领域概念
   - 与本次需求强相关的代码区域

3. **输出"仓库认知摘要"**：用 3-6 行向用户简要确认你对项目和需求的理解，等用户确认（或纠正）后再进入 Phase 2。

---

## Phase 2 — 系统化灵魂拷问

**两个文件必读：**
- `references/question-tree.md` —— 10 个 branch 的完整问题树，按顺序展开
- `references/grilling-rules.md` —— 拷问期间的硬性规则与推荐话术（每次提问都要遵守）

工作方式：
- 按问题树的 10 个 branch 顺序进行，每个 branch 内下钻到所有疑点都解决，再进入下一个
- 每次只问一个问题，每个问题先给推荐答案
- 能从代码/文档查到答案的，直接说出查到的事实让用户确认，不要凭空提问
- 发现冲突立刻打断澄清；遇到模糊词汇立刻追问到具体指标
- 每个分歧点达成共识后简短复述"OK，那我们定为 X"再进入下一题

> 注：以上规则的完整版本（包括话术模板、压力测试套路）在 `references/grilling-rules.md`，提问前若拿不准如何措辞，先回去翻一眼。

---

## Phase 3 — 结束盘问的判定

只有同时满足以下条件才能进入 Phase 4：

- [ ] `references/question-tree.md` 中的 10 个 branch 所有问题都有了明确答案
- [ ] 没有遗留的"这个等会儿再说"的悬而未决项
- [ ] 与代码现状的所有冲突都已经做出了决策
- [ ] 用户已经明确说"可以了"或"开始生成 PRD"

任一未满足，**不得**进入产出。继续追问，或把无法当场敲定的事项明确标记为"本期不做"。

---

## Phase 4 — 产出 final-prd.md

读 `references/prd-template.md` 拿到完整模板和写作约束，然后：

1. **创建目录**（如不存在）：在工作仓库的**根目录**下创建 `.qiqskills/grill2prd/`。
2. **处理重名**：如果 `final-prd.md` 已存在，**不要覆盖**，先把旧文件改名为 `final-prd-<YYYYMMDD-HHMMSS>.md` 备份，再写新文件。
3. **按模板写入**。整份文档使用 `CONTEXT.md` 中的规范术语；不写具体文件路径和大段代码（原型产出的关键状态机/schema 片段例外，可内联并注明来源）。
4. **告知用户**：输出文档的绝对路径，并给出 50 字以内的产出摘要（如"已产出 final-prd.md，包含 N 条用户故事、M 个实现决策"）。

---

## 重要约束（贯穿所有 Phase）

- **不一次性输出 PRD 后再回头补问**：所有问题必须在 Phase 2 解决，Phase 4 是纯产出阶段。
- **不修改用户已有的 `CONTEXT.md` / ADR**：更新建议统一写在 `final-prd.md` 的"词汇表更新建议"小节，由用户决定是否合入。
- **PRD 里不写具体文件路径或大段代码**：保持 PRD 稳定性，避免信息过期。例外：原型阶段产出的关键状态机、schema、reducer 片段可内联并注明"来自原型"。
- **目录路径强制**：产出文档必须放在 `<工作仓库根目录>/.qiqskills/grill2prd/final-prd.md`，不要放到其他位置。
- **使用项目领域术语**：整份 PRD 使用 `CONTEXT.md` 中的规范名词；不一致的地方在产出前就要纠正。

---

## 触发示例

- 用户说"我想加一个评论审核功能" → 进入 Phase 1，扫描仓库后开始拷问。
- 用户贴出一份未完成的 PRD 草稿 → 把草稿当作 Phase 1 的输入，从草稿没回答清的地方开始拷问。
- 用户说"qiq-grill2prd"或"grill2prd"，并附上一段话 → 把这段话当作输入开始流程。

---

## 引用文件清单

- `references/repo-scan-checklist.md` —— Phase 1 自动扫描清单
- `references/question-tree.md` —— Phase 2 完整问题树（10 branch）
- `references/grilling-rules.md` —— Phase 2 硬性规则与话术模板
- `references/prd-template.md` —— Phase 4 PRD 模板与写作约束
